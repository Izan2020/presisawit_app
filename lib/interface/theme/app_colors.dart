import 'package:flutter/material.dart';

class AppColors {
  // Theme Colors
  static const Color primaryColor = Color.fromRGBO(232, 167, 25, 1);
  static const Color secondaryColor = Color.fromARGB(255, 246, 209, 130);
  static const Color backgroundColor = Color.fromARGB(255, 248, 248, 248);

  // Default Colors
  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color gray = Colors.grey;
  static const Color lightGray = Color.fromARGB(255, 226, 226, 226);
  static const Color darkGray = Color.fromARGB(255, 109, 109, 109);
  static const Color red = Colors.redAccent;
  static const Color yellow = Colors.yellow;
  static const Color blue = Colors.blueAccent;
  static const Color green = Colors.green;
}
